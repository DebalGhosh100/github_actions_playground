name: Push to master

on: push

jobs:
  first-job:
    name: First job
    runs-on: ubuntu-latest
    steps:
      - name: "First step"
        run: echo "Hello from first step"


  second-job:
    runs-on: ubuntu-latest
    name: Second Job
    needs: first-job
    steps:
      - name: First step
        run: ls -al
      - name: Second step
        run: echo "Hello world"

