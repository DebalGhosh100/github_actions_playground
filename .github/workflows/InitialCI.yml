name: Testing CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo "Hello from first step"
      - name: Second step
        run: echo "Hello from second step"
